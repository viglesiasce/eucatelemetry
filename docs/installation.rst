============
Installation
============

At the command line::

    $ easy_install eucatelemetry

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv eucatelemetry
    $ pip install eucatelemetry
